{
  "name": "dsh-mac-notification",
  "version": "0.1.0",
  "private": true,
  "description": "DSH Web GUI 插件：任务完成时弹出 macOS 系统通知",
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "lightningcss": "^1.32.0",
    "tsdown": "^0.22.2",
    "typescript": "^6.0.3"
  }
}
